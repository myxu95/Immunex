# Immunex - Core Dependencies
#
# This file specifies the exact versions needed for Immunex to function properly.
# For installation, you can use either:
#   pip install -r requirements.txt  (exact versions)
#   pip install -e .                 (flexible versions from setup.py)

# Core scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
PyYAML>=6.0

# MD analysis
MDAnalysis>=2.6.0

# Structure analysis
biopython>=1.81

# CDR recognition and antibody numbering (install via conda: conda install -c bioconda anarci)
# anarci>=1.3

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Workflow / graph utilities
tqdm>=4.65.0
networkx>=3.1

# HTTP requests and web API
requests>=2.28.0
